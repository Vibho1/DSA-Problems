#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n, k, q;
        cin >> n >> k >> q;
        
        vector<int>vec(n);
        
        for(int i = 0; i < n; i++) {
            cin >> vec[i];
        }
        long long ans = 0;
        int lastSeen = -1;
        
        for(int i = 0; i < n; i++) {
            if(vec[i] <= q) {
                if(lastSeen == -1) {
                    lastSeen = i;
                }
                
                if((i - lastSeen) + 1 >= k) ans += 
            }
            else lastSeen = -1;
        }
    }
    return 0;
}
