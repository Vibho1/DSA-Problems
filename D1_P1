#include <bits/stdc++.h>
using namespace std;

long long helper(long long n) {
    if(n == 0) return 1;
    return n * helper(n - 1);
}
int main() {

        long long n;
        cin >> n;
        cout<< helper(n) << endl;
        return 0;
}
